node_modules
dist
dist-ssr
*.local
.vite
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.log

# Design-Handoff bundle (reference, not shipped)
design-handoff/
src/*.zip
*.zip

# Local project-context notes (kept local, not shipped)
CLAUDE.md

# Legacy-site scrapes (research-only, not shipped)
scrape-out/

# Design-Tooling (lokal installiert, nicht Projektcode)
.claude/skills/
.impeccable/

# Python bytecode (scrape scripts)
__pycache__/
*.pyc

# TypeScript incremental build cache
*.tsbuildinfo
